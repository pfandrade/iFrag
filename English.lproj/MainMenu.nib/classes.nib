{
    IBClasses = (
        {
            CLASS = AMButtonBar; 
            LANGUAGE = ObjC; 
            OUTLETS = {delegate = id; }; 
            SUPERCLASS = NSView; 
        }, 
        {
            ACTIONS = {
                addServer = id; 
                addToFavorites = id; 
                copyAddress = id; 
                playGame = id; 
                refreshSelectedServers = id; 
                refreshServerList = id; 
                reloadServerList = id; 
                removeServers = id; 
                toggleInspector = id; 
                togglePlayers = id; 
            }; 
            CLASS = FirstResponder; 
            LANGUAGE = ObjC; 
            SUPERCLASS = NSObject; 
        }, 
        {CLASS = KBGradientOutlineView; LANGUAGE = ObjC; SUPERCLASS = NSOutlineView; }, 
        {
            CLASS = LRFilterBar; 
            LANGUAGE = ObjC; 
            OUTLETS = {originalSender = id; }; 
            SUPERCLASS = NSView; 
        }, 
        {CLASS = MActionButton; LANGUAGE = ObjC; SUPERCLASS = NSPopUpButton; }, 
        {
            ACTIONS = {add = id; cancel = id; }; 
            CLASS = MAddServerController; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                addButton = NSButton; 
                address = NSTextField; 
                image = NSImageView; 
                kind = NSPopUpButton; 
                serverListsTreeController = id; 
                subTitle = NSTextField; 
                title = NSTextField; 
            }; 
            SUPERCLASS = NSWindowController; 
        }, 
        {CLASS = MDragHandleButton; LANGUAGE = ObjC; SUPERCLASS = NSButton; }, 
        {CLASS = MInnerSplitView; LANGUAGE = ObjC; SUPERCLASS = NSSplitView; }, 
        {
            ACTIONS = {
                addServer = id; 
                addToFavorites = id; 
                playGame = id; 
                refreshSelectedServers = id; 
                refreshServerList = id; 
                reloadServerList = id; 
                showPreferences = id; 
                toggleInspector = id; 
                togglePlayers = id; 
            }; 
            CLASS = MMainController; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                filterBar = AMButtonBar; 
                gamesOutlineView = id; 
                mainWindow = NSWindow; 
                rightSplitView = MInnerSplitView; 
                serverListsController = id; 
                serverTreeController = id; 
                serversController = id; 
                serversTableView = id; 
                splitView = MThinSplitView; 
            }; 
            SUPERCLASS = NSObject; 
        }, 
        {
            CLASS = MOutlineView; 
            LANGUAGE = ObjC; 
            OUTLETS = {splitView = MThinSplitView; }; 
            SUPERCLASS = KBGradientOutlineView; 
        }, 
        {
            CLASS = MPlayersController; 
            LANGUAGE = ObjC; 
            OUTLETS = {playersTableView = id; }; 
            SUPERCLASS = NSArrayController; 
        }, 
        {
            CLASS = MServerListsController; 
            LANGUAGE = ObjC; 
            OUTLETS = {serverListsOutlineView = id; serversController = id; treeController = id; }; 
            SUPERCLASS = NSArrayController; 
        }, 
        {
            CLASS = MServersController; 
            LANGUAGE = ObjC; 
            OUTLETS = {serversTableView = id; }; 
            SUPERCLASS = NSArrayController; 
        }, 
        {
            CLASS = MSplitViewsDelegate; 
            LANGUAGE = ObjC; 
            OUTLETS = {mainController = id; }; 
            SUPERCLASS = NSObject; 
        }, 
        {
            ACTIONS = {toggleColumn = id; }; 
            CLASS = MTableColumnsController; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                columnsMenu = NSMenu; 
                serverListsController = NSTreeController; 
                serverListsOutlineView = NSOutlineView; 
                serversController = NSArrayController; 
                serversTableView = NSTableView; 
            }; 
            SUPERCLASS = NSObject; 
        }, 
        {
            ACTIONS = {copy = id; cut = id; paste = id; }; 
            CLASS = MTableView; 
            LANGUAGE = ObjC; 
            SUPERCLASS = NSTableView; 
        }, 
        {
            CLASS = MThinSplitView; 
            LANGUAGE = ObjC; 
            OUTLETS = {resizeHandle = id; }; 
            SUPERCLASS = NSSplitView; 
        }, 
        {
            CLASS = MToolbarController; 
            LANGUAGE = ObjC; 
            OUTLETS = {mainController = id; searchField = id; serversController = id; }; 
            SUPERCLASS = NSObject; 
        }, 
        {CLASS = NSObject; LANGUAGE = ObjC; }, 
        {
            ACTIONS = {saveAction = id; }; 
            CLASS = "iFrag_AppDelegate"; 
            LANGUAGE = ObjC; 
            OUTLETS = {window = NSWindow; }; 
            SUPERCLASS = NSObject; 
        }
    ); 
    IBVersion = 1; 
}